SELECT DISTINCT B.customer_name
FROM Depositer B
WHERE NOT EXISTS (
    SELECT branch_name
    FROM Branch
    WHERE branch_city = 'Delhi'
    AND branch_name NOT IN (
        SELECT branch_name
        FROM BankAccount
        WHERE accno = B.accno
    )
);
